{% macro item(title, value, icon) %}
<span data-toggle="tooltip" data-placement="left" title="{{ title }}">
    <div class="row icon-text">
        <div class="col-2 text-center">
            <i class="fa fa-2x {{ icon }}" aria-hidden="true"></i>
        </div>
        <div class="col-10">
            <p>{{ value }}</p>
        </div>
    </div>
</span>
{% endmacro %}
